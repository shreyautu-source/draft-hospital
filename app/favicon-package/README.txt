Favicon package generated by Favicon Generator

1. Extract all files to your website root (or /public for frameworks).
2. Add the HTML snippet from the generator to your <head>.
3. Clear browser cache if the old favicon still appears.
